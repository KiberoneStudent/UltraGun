/*
 * ============================================================================
 * ИНСТРУКЦИЯ ДЛЯ Тимура
 * ============================================================================
 *
 * Что делает этот файл
 * ---------------------
 * Здесь хранятся все «правила игры» в виде чисел: размеры экрана, сколько уровней
 * у каждого улучшения, базовые награды и время. Остальные скрипты читают эти
 * константы, чтобы не дублировать магические числа по всему проекту.
 *
 * Что нужно реализовать
 * ---------------------
 * При необходимости добавь новые поля в GAME_CONFIG (например, гравитацию монеты,
 * скорость пули, цены апгрейдов). Следи, чтобы у монеты было не больше 5 уровней,
 * у времени — 3, у пистолета — 10, как в задании лаборатории.
 *
 * Что написать в Cursor (Ctrl+K)
 * -----------------------------
 * «В файле config.js добавь таблицу цен для каждого уровня апгрейда монеты,
 * времени и пистолета, и экспортируй их в объекте GAME_CONFIG так, чтобы
 * upgrades.js мог их использовать.»
 *
 * ============================================================================
 */

(function () {
  "use strict";

  window.GAME_CONFIG = {
    canvasWidth: 800,
    canvasHeight: 600,

    maxCoinUpgradeLevel: 5,
    maxTimeUpgradeLevel: 3,
    maxPistolUpgradeLevel: 10,

    baseRoundSeconds: 30,
    secondsPerTimeUpgrade: 10,

    baseCoinsTossed: 1,
    baseMoneyPerHit: 10,

    gravity: 1200,
    coinTossMinVy: -520,
    coinTossMaxVy: -380
  };
})();
